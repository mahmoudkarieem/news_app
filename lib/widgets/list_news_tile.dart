import 'package:flutter/material.dart';

class ListNewsTile extends StatelessWidget {
  const ListNewsTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
